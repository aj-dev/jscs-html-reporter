<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>JSCS HTML Report</title>
    <link href="{{relativeReporterPath}}/jscs-html-reporter.css" rel="stylesheet">
</head>
<body>
<div id="wrapper">
    <header class="box-shadow">
        Errors: {{errorsCollection.length}}
        <span>JSCS HTML Reporter v{{htmlReporterVersion}}</span>
    </header>
    <ul class="errors box-shadow">
        {{#each errorsCollection as |error|}}
            <li class="error">
                <span class="error-header">
                    <span class="error-number">{{inc @index}}.</span>
                    <span>{{error.message}} - </span>
                    <span class="file">
                        <a href="{{error.fileName}}" target="_blank">{{error.fileName}}</a>
                        <span class="error-location"> (line: {{error.line}}, column: {{error.column}})</span>
                    </span>
                </span>
                <pre class="error-message hide">{{error.reason}}</pre>
            </li>
        {{/each}}
    </ul>
</div>
<script type="text/javascript" src="{{relativeReporterPath}}/toggle.js"></script>
</body>
</html>
